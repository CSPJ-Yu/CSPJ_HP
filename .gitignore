# Separate "manage-cspj-hp" (manage.cs-pj.com) Wrangler project that
# happens to live in this same folder. Already tracked in its own
# independent repository — not included here.
/node_modules/
/.wrangler/
/public/
/package.json
/package-lock.json
/wrangler.jsonc
